Unit tests for HappyOven

Run tests:

```powershell
C:\src\flutter\bin\flutter.bat test
```

What is included:
- `test/create_recipe_form_notifier_test.dart`: basic unit test for `CreateRecipeFormNotifier` (add ingrediente, set articulo/cantidad, cost calculations).

Notes:
- Ensure `flutter` is on the path or use the full path to `flutter.bat` shown above.
