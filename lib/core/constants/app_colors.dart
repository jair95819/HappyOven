import 'package:flutter/material.dart';

class AppColors {
  // Marrones (estructura / base)
  static const brownDark = Color(0xFF7B4F2E);
  static const brownMid = Color(0xFFA8714A);
  static const brownLight = Color(0xFFD4A47A);

  // Naranja (acción primaria)
  static const primary = Color(0xFFFF8C42);
  static const primaryLight = Color(0xFFFFD9BE);
  static const primaryXLight = Color(0xFFFFF3EB);

  // Beige / fondos
  static const surface = Color(0xFFF5F0E8);
  static const background = Color(0xFFEDE5D6);

  // Neutros
  static const textDark = Color(0xFF2C2C2A);
  static const textMid = Color(0xFF5F5E5A);
  static const textLight = Color(0xFFBFB5A0);

  // Semánticos
  static const danger = Color(0xFFA32D2D);
  static const dangerLight = Color(0xFFFCEBEB);
  static const success = Color(0xFF3B6D11);
  static const successLight = Color(0xFFEAF3DE);
  static const warning = Color(0xFF633806);
  static const warningLight = Color(0xFFFAEEDA);

  static const olive = Color(0xFFC8CA9E); // color del logo
  static const oliveDark = Color(0xFF4A4A38); // texto sobre olive
}
